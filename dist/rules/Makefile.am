orchidsconfdir = $(sysconfdir)/orchids
orchidsruledir = $(orchidsconfdir)/rules

orchidsrule_DATA = \
  pid_tracker.rule \
  apachessl.rule \
  portscan.rule \
  dhcp_lease_check.rule \
  password_sniff.rule \
  anti_ssh_scan.rule \
  lin24_ptrace_audit.rule

EXTRA_DIST = $(orchidsrule_DATA)
